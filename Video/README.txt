Simulation is begun and immediately paused. The controller is then started, continuing the simulation and starting the screen capture.
This runs until the Controller is manually stopped, letting the pendulum swing freely.
